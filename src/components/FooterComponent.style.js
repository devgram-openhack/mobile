import { StyleSheet } from 'react-native';

const footerComponentStyle = StyleSheet.create({
  container: {
    alignItems: 'center',
    backgroundColor: '#333',
    justifyContent: 'center',
    paddingHorizontal: 16,
    paddingVertical: 8
  },

  newPostIcon: {
    color: '#31c32e',
    fontSize: 40
  }
});

export { footerComponentStyle };