import { StyleSheet } from 'react-native';

const pageStyle = StyleSheet.create({
  background: {
    height: '100%',
    width: '100%',
  },

  container: {
    backgroundColor: 'rgba(0, 0, 0, 0.8)',
    flex: 1,
  },
});

export { pageStyle };